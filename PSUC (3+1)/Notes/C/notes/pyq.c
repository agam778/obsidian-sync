#include <stdio.h>

int main(void)
{
    char name[50];
    int u;

    printf("Enter name: ");
    scanf("%s", &name);

    printf("Enter number of units: ");
    scanf("%d", &u);

    switch (u)
    {
        case 0 ... 200:
            
    }
}