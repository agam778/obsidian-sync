#include <stdio.h>

int main()
{
    int age = 5;
    if (age < 10)
    {
        if (age < 7)
        {
            printf("Less than 7");
        }
        else
        {
            printf("greater than 7, less than 10");
        }
    }
}