#include <stdio.h>

int main(void)
{
    int calls;
    float bill;

    printf("Enter calls: ");
    scanf("%d", calls);

    switch(calls <= 150 ? 0 : calls <= 250 ? 0.9 : calls <= 400 ? 1.2 : 1.5) {
        case 0:
        case 
    }
}