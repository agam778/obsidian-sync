#include <stdio.h>

int main()
{
    int m, y, d = 0;
    printf("Enter months: ");
    scanf("%d", &m);
    printf("Enter year: ");
    scanf("%d", &y);
    if (m >= 1)
        d += 31;
    if (m >= 2)
        
}